# Restate v1.7.10 Release Notes

## Highlights

- **Automatic VQueue metadata cleanup**: Fully empty, unpaused queues no longer retain obsolete metadata.
- **Opt-in cleanup of historical metadata**: Reclaim obsolete VQueue metadata accumulated by earlier versions.
- **More efficient VQueue metadata writes**: Periodic full writes shorten metadata merge chains, while disabling the default RocksDB merge threshold avoids repeated chain counting and threshold-triggered value reconstruction on the write path.

---

## Table of Contents

- [Bug Fixes](#bug-fixes)
  - [Automatically Clean Up Obsolete VQueue Metadata](#automatically-clean-up-obsolete-vqueue-metadata)
- [Improvements](#improvements)
  - [More Efficient VQueue Metadata Writes](#more-efficient-vqueue-metadata-writes)

---

## Bug Fixes

### Automatically Clean Up Obsolete VQueue Metadata

Restate now deletes a VQueue's metadata when an update leaves the queue unpaused and fully empty, including its finished-entry stage. This prevents workloads with many short-lived queues from accumulating obsolete metadata, limiting partition-store growth and avoiding increasingly expensive metadata scans.

**Impact on users:**

- Fully empty, unpaused queues disappear from `sys_vqueue_meta` and the VQueue CLI.
- Historical timestamps and exponential moving averages stored in deleted metadata are discarded. If the same queue is created again, its statistics restart.
- Empty paused queues retain their metadata and pause state.

Automatic cleanup during queue updates requires no configuration.

**Cleaning up existing metadata**

A separate, one-time local storage migration removes obsolete metadata accumulated by earlier versions. This migration is opt-in for v1.7.10 and v1.8.

After upgrading all cluster nodes to v1.7.10 or newer, enable it on each node:

```toml
[common]
experimental-enable-vqueue-obsolete-cleanup = true
```

Each local partition-store replica scans its metadata in bounded, low-priority chunks and records completion to avoid repeating the scan. The first startup with cleanup enabled may take longer, and deleting a large backlog can temporarily increase RocksDB compaction and disk I/O.

For large backlogs, roll out the setting gradually across replicas, allowing cleanup and subsequent compaction to settle before restarting additional replicas.

**Downgrade restriction:** A partition store that has completed this migration requires Restate v1.7.10 or newer.

Restate v1.9 enables this migration unconditionally.

_Related: [inline cleanup](https://github.com/restatedev/restate/commit/19aa6754deeccb86f76639f04353ad2014c93130), [historical cleanup](https://github.com/restatedev/restate/commit/267fc83fe55272699973a3ca26e11968c74c1729)_

---

## Improvements

### More Efficient VQueue Metadata Writes

Restate now periodically writes complete VQueue metadata instead of appending another RocksDB merge operation. Full writes occur for approximately 1% of updates and when an update follows more than one hour without tracked queue activity. These writes shorten metadata merge chains, reducing the work needed to reconstruct metadata during reads, flushes, and compactions.

Alongside this change, the default `worker.storage.rocksdb-max-successive-merges` changes from `5000` to `0`, avoiding repeated merge-chain counting and threshold-triggered value reconstruction on the write path.

Existing explicit settings remain effective. To retain the previous threshold:

```toml
[worker.storage]
rocksdb-max-successive-merges = 5000
```

_Related: [metadata write optimization](https://github.com/restatedev/restate/commit/30346e7b794416504508814cb9a6bc717e55fd75)_
